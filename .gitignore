# ツールの state とログはリポジトリに来ない。stow を --no-folding で張るので
# ~/.config/<tool> が実ディレクトリになり、ツールの書き込みはそちらへ落ちる。
# 折り畳まれた symlink だった間だけリポジトリの中に溜まっていた。

**/*.local
# stow パッケージの .local ディレクトリ (~/.local へのリンク元) は追跡する。
# 上の **/*.local はローカル上書きファイル向けだが、ディレクトリ名にもマッチする。
!**/.local/
