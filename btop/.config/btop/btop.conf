#? Config file for btop v. 1.4.7

# 配色は vanadis が生成する themes/vanadis.theme を使う。btop は
# ~/.config/btop/themes と share/btop/themes を名前で検索する。
# 名前を固定にしてあるので、テーマを切り替えてもここは動かない。
color_theme = "vanadis"

# テーマが自前の背景色を持つので有効にする。
theme_background = True

truecolor = True
